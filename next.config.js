/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos', 'images.unsplash.com', 'api.dicebear.com', 'api.multiavatar.com'],
    },
}

module.exports = nextConfig

